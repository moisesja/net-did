# Follow-up Review: Verify Status of Previously Reported did:webvh Issues

## Context

- Date: 2026-03-14
- Scope: verify whether the previously reported `did:webvh` issues have been addressed in the current codebase and in GitHub issue state
- Prior issues under review: #14 through #21
- Note: `tasks/lessons.md` is still not present in this repository

## Checklist

- [x] Confirm repository state is clean before review
- [x] Re-read the previously reported issue set
- [x] Inspect current implementation for each issue
- [x] Inspect current tests for regression coverage of each issue
- [x] Check GitHub issue state for each issue
- [x] Classify each issue as fixed, partially fixed, or still open
- [x] Record supporting evidence and final conclusions

## Working Notes

- `dotnet test netdid.sln --no-restore` passed: 573 tests, 0 failures.
- GitHub issue state as of review:
  - `#14` closed
  - `#15` closed
  - `#16` closed
  - `#17` open
  - `#18` closed
  - `#19` closed
  - `#20` closed
  - `#21` closed
- Follow-up action taken:
  - Reopened `#15` with a clarification comment because the witness fix is only partial.
  - Left `#17` unchanged because it is already open and was explicitly excluded from reopen actions.

## Findings

- `#14` Fixed. Entry hashing now chains via `previousEntry.VersionId` in both update/deactivate generation and validator logic, with regression tests.
- `#15` Partially fixed. Missing/malformed witness files now fail, and spec-array parsing was added. However, resolution still validates only the target entry's witness proofs and does not implement the "later proofs can satisfy earlier witnessed versions" rule.
- `#16` Fixed. Resolution now rejects a hosted log whose document `id` does not match the requested DID.
- `#17` Still open. The API still cannot accept witness proofs or emit `did-witness.json` artifacts.
- `#18` Fixed. README now marks `did:webvh` as implemented, and the conformance report is explicitly framed as DID Core coverage.
- `#19` Fixed. `DidDocumentMetadata` now includes `VersionTime`, and create/resolve populate it.
- `#20` Fixed. Missing `versionId` / `versionTime` now return `notFound`, and partial-chain validation allows earlier valid versions to resolve.
- `#21` Fixed. Pre-rotation now requires fresh `updateKeys`, enforced in both update and chain validation paths.
