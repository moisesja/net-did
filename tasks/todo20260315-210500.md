# Follow-up Review: Revalidate Issues #15 and #29

## Context

- Date: 2026-03-15
- Scope: verify whether issues `#15` and `#29` now fully satisfy the previously identified compliance concerns
- Focus:
  - current implementation
  - regression tests
  - current GitHub issue state
  - current spec expectations
- Note: `tasks/lessons.md` is still not present in this repository

## Checklist

- [x] Create a dedicated revalidation note
- [x] Inspect current implementation for `#15` and `#29`
- [x] Inspect relevant regression tests
- [x] Check current GitHub issue state and recent discussion
- [x] Compare current behavior against the relevant specs
- [x] Record final classification and actions

## Working Notes

- `#15` now implements cumulative witness coverage in `WitnessValidator.ValidateAllWitnesses(...)` and has a direct regression test proving that a later proof can satisfy earlier witnessed versions.
- `#15` also continues to fail resolution on missing/malformed witness files and rejects the old legacy single-object witness format.
- `#29` is much closer than before:
  - service queries default to a filtered DID Document
  - `serviceType` is supported
  - set endpoints are supported for `text/uri-list`
  - `verificationRelationship` is now part of `DidUrlDereferencingOptions`
- `#29` still has two algorithm-level gaps:
  - unsupported `Accept` values still succeed instead of returning `representationNotSupported`
  - service-id matching does not normalize relative references to absolute URIs before comparison
- Targeted verification passed:
  - `dotnet test tests/NetDid.Core.Tests/NetDid.Core.Tests.csproj --no-restore` -> `292 passed`
  - `dotnet test tests/NetDid.Method.WebVh.Tests/NetDid.Method.WebVh.Tests.csproj --no-restore` -> `70 passed`

## Findings

- `#15`: fixed and now meets the previously-open concern.
- `#29`: still not fully fixed; should be reopened with updated rationale.

## Actions Taken

- Left `#15` closed after confirming the cumulative witness-coverage fix and regression test.
- Reopened `#29` and added follow-up comment:
  - `https://github.com/moisesja/net-did/issues/29#issuecomment-4064719985`
