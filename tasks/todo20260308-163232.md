# TODO 2026-03-08 16:32:32

- [x] Capture the current workspace state and closed-issue status for issues #2 through #7.
- [x] Review the code and documentation paths previously implicated by each issue.
- [x] Re-run targeted reproductions and solution/tests needed to validate each fix.
- [x] Record whether each prior finding is fully fixed, partially fixed, or still reproducible.
- [x] Summarize validation evidence, residual risks, and blockers in the review section.

## Review

- Verified issue state on GitHub: issues `#2` through `#7` are all `CLOSED`.
- Reviewed the corresponding fix history on `main`:
  - `b4b1a61` parser validation
  - `2cc8116` JWK leak fix
  - `de806d8` JSON-LD context handling
  - `08f8c87` embedded VM dereferencing
  - `3b2b70c` numalgo 4 fidelity
  - `2d5892d` / `b16c280` documentation alignment
- Re-ran the full test suite with `dotnet test netdid.sln`: all tests passed.
  - `NetDid.Core.Tests`: `233/233`
  - `NetDid.Method.Key.Tests`: `22/22`
  - `NetDid.Method.Peer.Tests`: `25/25`
- Re-ran targeted regression coverage:
  - `dotnet test tests/NetDid.Core.Tests/NetDid.Core.Tests.csproj --filter "FullyQualifiedName~DidParserTests|FullyQualifiedName~DidDocumentSerializerTests|FullyQualifiedName~DefaultDidUrlDereferencerTests"` → `84/84` passed
  - `dotnet test tests/NetDid.Method.Peer.Tests/NetDid.Method.Peer.Tests.csproj --filter "FullyQualifiedName~DidPeerMethodTests"` → `22/22` passed
- Validation result by prior issue:
  - `#2` fixed: `DidParser` now rejects DID URLs/illegal characters as bare DIDs and models URL parameters via `DidUrl.Parameters`.
  - `#3` fixed: `DidDocumentSerializer.WriteJwk` now emits public members only, and regression tests assert `d` is absent.
  - `#4` fixed: object-valued JSON-LD contexts now serialize and deserialize as `JsonElement` entries.
  - `#5` fixed: dereferencing now searches embedded verification methods across all relationship arrays.
  - `#6` fixed: AGENTS/README/PRD now state current implementation vs planned methods, and PRD explicitly says W3C conformance is planned, not implemented.
  - `#7` fixed: numalgo 4 now rewrites controller + embedded VMs and preserves `Context` / `AdditionalProperties`.
- Residual risk:
  - This validation was scoped to the six previously reported findings and their regression coverage. It was not a brand-new repo-wide bug hunt for unrelated issues introduced after those fixes.
