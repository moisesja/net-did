# Remediate GHSA-3vhp-h4rr-r254

- [x] Read the draft advisory, confirm the current `main` baseline, and map all five findings to production and test code.
- [x] Add failing security regression tests for:
  - prior-effective witness-policy enforcement when an update lowers, removes, enables, or replaces witnessing;
  - invalid-before-valid witness proofs, duplicate votes, and exact verified-signer authorization;
  - loopback/private/link-local/unspecified IPv4 and IPv6 literals, unsafe DNS answers, mixed DNS answers, redirect blocking, and the DI transport path;
  - invariant Gregorian timestamp formatting/parsing across cultures and proof creation;
  - authenticated fractional-second version selection and rejection of mirror-modified `versionTime` values.
- [x] Fix witness enforcement by deriving each entry's authorizing witness configuration from genesis-own/first-activation/prior-effective parameters, using the same rule for witness-file fetch gating, and assigning/deduplicating weight only after a valid proof is bound exactly to its verified `did:key` signer.
- [x] Fix resolver SSRF by rejecting prohibited IP literals, resolving and vetting every DNS answer inside a pinned `SocketsHttpHandler.ConnectCallback`, connecting only to a vetted address, disabling redirects and proxies, and applying the secure primary handler to dependency-injection registration.
- [x] Make hash/proof/selection timestamps deterministic and authenticity-preserving by centralizing invariant UTC formatting/parsing with optional full fractional precision, using it for log serialization, proof creation, deserialization, and version-time queries; make adjacent version-number parsing/formatting invariant as well.
- [x] Update `NetDidPRD.md` with the witness-transition, signer-binding, network egress, redirect, and timestamp invariants.
- [x] Run focused security tests, all did:webvh and DI tests, the full solution suite, formatting/build checks, and compare the final behavior/diff against the baseline.
- [x] Run an independent adversarial review of the completed patch, address any findings, and repeat affected validation.
- [x] Update `CHANGELOG.md` under Unreleased Security with all five remediations after validation succeeds.
- [x] Record exact test/build/adversarial-review evidence in the review section below.

## Review results

- Baseline: current `main` (`dfee879`) passed the original 145-test did:webvh suite before edits.
- Focused regression pass: 134/134 tests passed across serializer, timestamp, witness, URL mapper,
  default HTTP client, and network-policy test classes.
- Component passes: `NetDid.Method.WebVh.Tests` 232/232 and
  `NetDid.Extensions.DependencyInjection.Tests` 12/12.
- Initial pre-PR full solution: 875/875 tests passed. After rebasing onto the latest `main` and
  addressing PR review feedback, the complete solution passed 894/894 tests
  (`dotnet test netdid.sln --no-restore --tl:off`).
- Release build: succeeded with 0 warnings and 0 errors
  (`dotnet build netdid.sln -c Release --no-restore --tl:off -m:1`).
- Three independent adversarial reviews exercised witness transitions/signer confusion,
  timestamp culture/precision/wire normalization, SSRF literals/IDNA/DNS rebinding/IPv6
  transitions, and DI/API integration. They found and prompted fixes for first witness activation,
  exact timestamp-token authentication, malformed time-query fallback, IDNA-disguised local
  hosts, special IPv6 transition ranges, stale comments, and missing lowering/replacement/fractional
  selection regressions. Final re-reviews reported no remaining concrete findings.
- `git diff --check` passed. The build-generated final-newline change to
  `w3c-conformance-report.md` was restored; unrelated pre-existing untracked task files were left
  untouched.
