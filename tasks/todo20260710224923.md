# PR #86 review follow-up and merge repair

PR: https://github.com/moisesja/net-did/pull/86

## Plan

- [x] Rebase the PR branch onto the latest `origin/main` and resolve the WebVH timeout/SSRF integration conflicts without weakening either hardening change.
- [x] Document the intentional breaking rejection of non-public `did:webvh` hosts, including Create/Resolve scope and the supported custom-client testing/deployment pattern.
- [x] Normalize malformed fetched-log `versionTime` values to `invalidDidLog` and add a regression test for non-UTC offsets.
- [x] Clean up the review nits: clarify the unused single-entry witness overload and reconcile validation evidence.
- [x] Record non-blocking spec/deployment follow-ups as GitHub issues without expanding this PR's behavioral scope.
- [x] Run focused tests, the complete suite, Release build, and diff hygiene checks.
- [x] Run adversarial review over the merged transport and timestamp/error-mapping paths; address any findings.
- [x] Update the PR branch and verify GitHub reports it mergeable with passing checks.

## Review

- Rebased onto `origin/main` at `ec6a56c`, combining the #80 total-fetch timeout with the GHSA
  secure handler in both parameterless and DI construction paths.
- Documented the intentional Create/Resolve breaking policy for localhost and non-public IP
  literals, the supported public-looking-host fake/allowlisted-fixture pattern, NAT64/proxy caveats,
  and the legacy Update/Deactivate migration asymmetry. Corrected the README constructor example.
- A fetched log entry with a non-UTC or malformed `versionTime` now returns `invalidDidLog`; the
  catch is scoped to the fetched-log parse boundary so local write-path exception contracts do not
  change.
- Adversarial merge review found and prompted two additional fixes: caller cancellation now wins
  before URI security preflight, and finite timeout values above `CancelAfter`'s portable range are
  rejected during options construction. Final transport and timestamp re-reviews were clean.
- Filed non-blocking specification follow-ups: #87 (strictly increasing `versionTime`) and #88
  (distinct witness IDs, threshold bounds, and config validation).
- Focused validation: 90 WebVH review/security tests and 14 DI tests passed.
- Full solution: 894/894 tests passed.
- Release build: succeeded with 0 warnings and 0 errors.
- PR #86 was force-pushed with lease after the rebase; GitHub reported `MERGEABLE` / `CLEAN` and
  the `build-and-test` workflow passed on the updated branch.
