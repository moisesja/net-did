# Promote recurring tasks-folder workflows into reusable skills

## Context

- **Date:** 2026-07-12
- **Scope:** Distill the recurring workflows found across `tasks/` (37 todo files, 12 issue specs, 46 lessons) into reusable Claude Code skills under `.claude/skills/`.
- **Decision (user-approved):** author 5 skills, project-scoped (`.claude/skills/`):
  `net-did-fix`, `net-did-verify`, `net-did-release`, `adversarial-review`, `spec-conformance-audit`.
- **Design rule:** encode only net-did-specific conventions on top of the generic built-in skills
  (`/code-review`, `/security-review`, `/verify`, `/review`) — do not re-implement them.

## Verified repo facts (baked into the skills)

- Solution: `netdid.sln`. Src packages (5, all published): `NetDid.Core`, `NetDid.Method.Key`,
  `NetDid.Method.Peer`, `NetDid.Method.WebVh`, `NetDid.Extensions.DependencyInjection`.
- Test projects: `NetDid.Core.Tests`, `NetDid.Method.Key.Tests`, `NetDid.Method.Peer.Tests`,
  `NetDid.Method.WebVh.Tests`, `NetDid.Extensions.DependencyInjection.Tests`, `NetDid.Tests.W3CConformance`.
- Samples: `NetDid.Samples.DidKey`, `NetDid.Samples.DidPeer`, `NetDid.Samples.DidWebVh`, `NetDid.Samples.DependencyInjection`.
- Version: `Directory.Build.props` `<NetDidVersion>` (currently 2.2.0).
- CI: `.github/workflows/ci.yml` (restore/build/test on push+PR to main).
- Publish: `.github/workflows/publish.yml` triggers on `v*` tag push → build/test/pack/push all packages to NuGet.
- Conformance oracle: `w3c-conformance-report.md` regenerated by `NetDid.Tests.W3CConformance` (restore its timestamp churn).

## Plan

- [x] Read lessons, all issue specs, representative todo, changelog, conformance report
- [x] Fan out subagents to extract recurring workflows from the 37 todo files
- [x] Verify exact commands / project names / CI / version location from the repo
- [x] Author `.claude/skills/net-did-fix/SKILL.md`
- [x] Author `.claude/skills/net-did-verify/SKILL.md`
- [x] Author `.claude/skills/net-did-release/SKILL.md`
- [x] Author `.claude/skills/adversarial-review/SKILL.md`
- [x] Author `.claude/skills/spec-conformance-audit/SKILL.md`
- [x] Review section

## Review

**Done — 5 skills authored under `.claude/skills/` (project-scoped, checked in):**

| Skill | Encapsulates | Source pattern (files) |
|---|---|---|
| `net-did-fix` | issue-fix cycle: assess-with-citations → branch → fail-first `IssueNN_*` tests → verify → adversarial → docs+CHANGELOG (no version bump) → PR, stop-before-merge | ~15 todo files (dominant) |
| `net-did-verify` | done-gate: 0-warning Release build, per-project test counts, W3C conformance green, samples end-to-end, diff hygiene | ~13–15 files |
| `net-did-release` | semver → `chore/release` → `NetDidVersion` bump → CHANGELOG stamp → verify → tag `vX.Y.Z` → publish.yml → confirm 5 pkgs on NuGet | 3 files |
| `adversarial-review` | mandatory red-team pass encoding the 46 `lessons.md` rules (TOCTOU snapshot-once, exclusive postconditions, normative-spec-before-claims, private-JWK leak, EC-point validation, both-way knobs, SSRF) | ~8 files + lessons |
| `spec-conformance-audit` | audit→one-issue-per-finding: claims review → green baseline → repros → normative-spec cross-check → severity+file:line → residual-risk | 3–4 files |

Design rule honored: each skill layers **net-did-specific** conventions on top of the built-in `/code-review`, `/security-review`, `/verify`, `/review` rather than re-implementing them, and cross-references the others (`net-did-fix` calls `net-did-verify` + `adversarial-review`; `net-did-release` calls `net-did-verify`).

Validation: all 5 `SKILL.md` files parse (name == dir name; descriptions 287–477 chars, well under limit). Auto-discovered from `.claude/skills/`.

Not promoted (lower recurrence): fix-verification/issue-revalidation loop (tri-state), PR-review own-PR-COMMENTED convention, library-migration + upstream-gap paired doc. Noted for a future pass if wanted.

No `CHANGELOG.md` entry (internal tooling, not a library change); no `NetDidVersion` bump.

## Follow-up (same session): wire skills into docs + prune stale tasks/

**Docs updated so the skills actually get leveraged** (CLAUDE.md is a symlink → AGENTS.md, so one edit covers both):
- `AGENTS.md` — added a `## Workflow Skills` table mapping each skill to when-to-invoke + what-it-encodes, plus inline pointers in the principle sections (Subagent Strategy → `adversarial-review`; Verification Before Done → `net-did-verify`; Task Management → `net-did-fix`/`net-did-release`).
- `CONTRIBUTING.md` — added a `## Working with Claude Code` note pointing humans to the skills + `tasks/lessons.md`.
- `net-did-fix` skill made self-contained (dropped its pointer to a since-deleted todo).

**tasks/ pruned 51 → 10 files.** Deleted 41 completed working-docs (33 `todo*.md` whose work shipped + 5 stale issue follow-up/regression notes on now-closed issues + 3 untracked completed todos). Verified before deleting: only issues #35 and #30 remain open; `v2.2.0` is tagged on origin and NetDid.Core 2.2.0 is live on NuGet (so the untracked NuGet-deploy todo's open items were already satisfied by the tag-driven publish).

**Kept:** `lessons.md` (living), this plan, the 6 `issue-audit-*.md` + peer-DID feature draft (reusable issue templates the audit skill points to), and `upstream-gaps-*.md`.
**Flagged for the user:** `upstream-gaps-20260613-100115.md` is the companion to the now-completed crypto→NetCrypto migration; its lead blocker (DP-1: DataProofsDotnet.Core unpublished) is resolved (2.2.0 ships against the now-published prerelease). If the remaining upstream gaps were already filed against NetCrypto/DataProofsDotnet, it can be deleted too.

Tracked deletions are git-recoverable (`git checkout HEAD -- tasks/<file>`); the 3 untracked deletions were read first and confirmed complete.
