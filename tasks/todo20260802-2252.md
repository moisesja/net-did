# Issue #119 — did:ethr archive-node docs + opt-in RPC auto-configuration with endpoint probing

## Context

`KnownNetworks` catalogues the official ERC-1056 deployments but every entry ships `RpcUrl = ""`, and nothing documents that did:ethr resolution requires an endpoint serving **historical `eth_getLogs`** (archive-grade). Users on pruned free-tier endpoints get opaque resolve-time failures. The reference-resolver maintainer's guidance (quoted in the issue): probe candidate public endpoints with known-old DIDs and discard any that return empty logs. Their companion JS library is **not yet published** (verified by web search 2026-08-02), so our probe data is designed to be alignable later, per the issue's own suggestion.

Assessment confirmed against source:
- `src/NetDid.Method.Ethr/Rpc/KnownNetworks.cs:8-13,152-156` — empty `RpcUrl`, `with {}` instruction, no quality guidance.
- `src/NetDid.Method.Ethr/Rpc/EthereumNetworkConfig.cs:7` — `RpcUrl` XML doc says nothing about archive requirements.
- `src/NetDid.Method.Ethr/DidEthrMethod.cs:597-604` — walker fails closed (`IncompleteEventHistoryException`) when an asserted block has no logs; mapped at `:216-232` to `internalError` with "pruned/non-archive" text. Detection exists, but only at resolve time, per DID.
- Live demonstration of the failure class: `https://ethereum-rpc.publicnode.com` answers `eth_chainId` fine but rejects a historical `eth_getLogs` with "Archive requests require a personal token" — a liveness check alone passes an unusable endpoint.
- Real probe data is obtainable and verifiable: mainnet legacy registry (`0xdca7ef03…`) has ERC-1056 events for identity `0xf87e64e1fd8098fde28fb0852459dc839f35de94` at blocks 10,001,725/10,001,739 (May 2020), confirmed via `eth_getLogs` on `eth.drpc.org` during assessment.

Severity: Medium (documentation gap + missing batteries-included setup; failures are already fail-closed, never silent truncation).

## Approach decision

Implement the helper **inside `NetDid.Method.Ethr`** as a static class, not a new package. The package already references `Microsoft.Extensions.Logging.Abstractions` + `Microsoft.Extensions.Http`, and probing reuses the existing hardened `DefaultEthereumRpcClient` (response caps, bounded error extraction) — so acceptance criterion "no new mandatory dependencies" is met with zero packaging overhead. The issue explicitly allows "a static helper".

Registration in DI is eager/synchronous (`NetDidBuilder.AddDidEthr`, `src/NetDid.Extensions.DependencyInjection/NetDidBuilder.cs:103`), so the helper is a **probe-first, then register** bootstrap: caller awaits it, gets ready `EthereumNetworkConfig`s, hands them to the existing `AddDidEthr(IEnumerable<EthereumNetworkConfig>)` or `DidEthrMethod` ctor. No DI changes needed.

## Plan

### 1. New: `src/NetDid.Method.Ethr/Rpc/EthrRpcAutoConfig.cs`

```csharp
public static class EthrRpcAutoConfig
{
    // Built-in candidate public endpoints for major official deployments
    // (mainnet, sepolia, gnosis, polygon — each verified working at implementation time).
    public static IReadOnlyDictionary<string, IReadOnlyList<string>> DefaultCandidateEndpoints { get; }

    public static Task<IReadOnlyList<EthereumNetworkConfig>> ConfigureAsync(
        IReadOnlyDictionary<string, IReadOnlyList<string>>? candidateEndpoints = null, // null => defaults
        ILogger? logger = null,
        TimeSpan? perEndpointTimeout = null,   // default ~10 s; owned HttpClients, so both directions work
        CancellationToken ct = default);
}
```

Behavior per network (keys resolved via `KnownNetworks.Find`; unknown names rejected with a logged reason):
1. Snapshot the caller-supplied dictionary/lists ONCE at entry (interface-typed input = adversarial; lessons).
2. For each candidate URL **sequentially, first-healthy-wins** (minimizes traffic; deterministic):
   a. `eth_chainId` must equal the network's `ChainId` → else discard, log fixed-text reason + the URL.
   b. **Historical-logs probe**: `eth_getLogs` on the network's registry address filtered to a hard-coded known-old identity + small block window (`HistoricalLogProbe` record: identity, fromBlock, toBlock; window ≤ a few hundred blocks to stay under provider range limits). Non-empty → endpoint passes; empty or RPC error → discard, log fixed-text reason.
3. Network with a passing endpoint → emit `KnownNetworks.<X> with { RpcUrl = url }`.
4. Network with **no probe data** (we only hard-code probes we can verify on-chain) → chain-ID check only; emit config but log a warning that archive-grade serving is unverified.
5. Network with no passing candidate → omitted from the result, logged.
6. Overall: token checked before/after every RPC await (per-dependency-await symmetry lessons); per-endpoint failures never abort the whole run; logging guarded with a final swallow (never-throw precedent of `LogResolveFailure`); log text is fixed library-owned strings + the candidate URL (caller/library-supplied) — never response content.

Probe table: hard-code only entries verified live during implementation via real `eth_getLogs` (mainnet entry above; attempt the same for sepolia/gnosis/polygon and include whichever verify). Each entry carries a comment citing the verification (identity, block, date checked). **No fabricated on-chain data** (emulator/mock lessons).

Testability seam: `internal` overload accepting `Func<EthereumNetworkConfig, IEthereumRpcClient>` (per-candidate client factory), used via existing `InternalsVisibleTo NetDid.Method.Ethr.Tests`. Public path builds one owned `DefaultEthereumRpcClient` per candidate URL (`HttpClient { BaseAddress, Timeout }`), disposed after probing.

Trust property documented on the class: probing verifies **availability/quality only** — a passing endpoint is still a single untrusted RPC node that can forge self-consistent history (the library's existing documented integrity property).

### 2. Docs — archive requirement at every `RpcUrl` supply point

- `EthereumNetworkConfig.cs:7` — extend `RpcUrl` XML doc: resolution requires historical `eth_getLogs` (archive-grade); pruned endpoints fail closed at resolve time; point to `EthrRpcAutoConfig`.
- `KnownNetworks.cs` class + `All` XML docs — same requirement + pointer.
- `README.md`:
  - new `### Archive nodes and endpoint auto-configuration` subsection after `### Known networks` (insert ~line 557): the requirement, the publicnode failure example shape, `EthrRpcAutoConfig.ConfigureAsync` usage snippet, trust caveat.
  - cross-refs: `:546` (Known networks "All entries have RpcUrl=…"), `:348-357` (pruned-node error prose → point at new subsection), `:783-787` (DI `AddDidEthr` dictionary overload).
- `NetDidPRD.md` §8.8 (ends ~line 1661): add numbered requirements — archive-grade endpoint requirement (MUST for historical completeness), auto-config helper behavior (probe semantics, first-healthy-wins, fail-closed discard, unverified-network warning), trust scope (availability only).
- `CHANGELOG.md` — `### Added` entry at top (`[Unreleased]`), issue #119 style-matched. **No `NetDidVersion` bump.**

### 3. Tests — `tests/NetDid.Method.Ethr.Tests/Issue119AutoConfigTests.cs`

`Issue119_*` naming, box-banner grouping, NSubstitute `IEthereumRpcClient` via the internal seam, hand-rolled recording logger (Issue116 style — repo has no FakeLogger package):
- pruned endpoint (chainId OK, `GetLogsAsync` → empty) → discarded + logged actionable reason (acceptance criterion 3).
- wrong chainId → discarded.
- RPC error / cancellation-ignoring hang (via timeout) → discarded, run continues to next candidate.
- healthy endpoint → config emitted with that URL; earlier-listed healthy endpoint wins.
- network without probe data → emitted with unverified warning.
- no passing candidate → network omitted + logged.
- caller cancellation propagates (including mid-probe).
- hostile snapshot: candidate list mutating between enumerations → probed set is the entry snapshot.
- throwing logger → helper still completes (guarded logging).
- `DefaultCandidateEndpoints` covers only networks present in `KnownNetworks.All`.

Feature tests are fail-first trivially (surface doesn't exist before the change); noted in the Review section rather than stash-proven.

### 4. Workflow (net-did-fix skill)

- Branch `feat/issue-119-archive-docs-autoconfig` off `main`; copy this plan to `tasks/todo{timestamp}.md`.
- Implement (steps 1–2), tests (step 3).
- Live spot-check of the built-in default endpoints + probe data (curl, not CI-coupled).
- **Verify**: `net-did-verify` (0-warning Release build, full suite per-project counts, W3C conformance, samples offline end-to-end — sample untouched by design, auto-config needs network).
- **Adversarial review** skill on the diff (new network-facing surface: hostile endpoint responses, log injection, snapshot/TOCTOU, cancellation symmetry, timeout both-directions).
- PR with `Fixes #119`; **stop at open PR** (user merges).

## Out of scope (justified)

- New `NetDid.Method.Ethr.AutoConfig` package — unnecessary; zero new deps in-package satisfies the criterion the package split existed to protect.
- Aligning probe DIDs with the maintainer's companion library — not yet published; probe table is data-driven so a follow-up can swap entries. Tracked in the issue itself ("track when published").
- Changing resolve-time error surfacing (`internalError` vs `notFound`) — companion issue, separate scope.
- Sample changes — auto-config requires live network; samples must keep running offline for `net-did-verify`.
- Background re-probing / caching of probe results — bootstrap-time helper only; callers re-run it on their own schedule.

## Verification

1. `dotnet build -c Release` — 0 warnings.
2. `dotnet test` per project — all green, record counts; new `Issue119_*` tests green.
3. W3C conformance suite green; samples run end-to-end (offline, unchanged).
4. Live smoke: run `ConfigureAsync` against `DefaultCandidateEndpoints` once from a scratch console/test (manual, not CI): expect publicnode-class endpoints discarded with the logged archive reason, ≥1 network configured.
5. `git diff --check` clean.

## Review (2026-08-03)

**PR:** https://github.com/moisesja/net-did/pull/129 (open — awaiting maintainer review; not merged)
**Branch:** `feat/issue-119-archive-docs-autoconfig`, commits `70e0f10` (feature) + `ab1040d` (adversarial round-1 fixes)

### Files changed
- `src/NetDid.Method.Ethr/Rpc/EthrRpcAutoConfig.cs` (new) — probe helper, probe table, defaults
- `src/NetDid.Method.Ethr/Rpc/EthereumNetworkConfig.cs`, `KnownNetworks.cs` — archive-requirement XML docs
- `src/NetDid.Extensions.DependencyInjection/NetDidBuilder.cs` — archive note on both `AddDidEthr` overloads
- `tests/NetDid.Method.Ethr.Tests/Issue119AutoConfigTests.cs` (new, 31 tests), `Issue109UnobservedTaskTests.cs` (inventory +`EthrRpcAutoConfig.cs = 2`)
- `README.md` (new section + 3 cross-refs), `NetDidPRD.md` (§8.9), `CHANGELOG.md` ([Unreleased] Added)

### Acceptance criteria re-check
- [x] Archive requirement documented at every `RpcUrl` supply point (config XML, KnownNetworks, both AddDidEthr overloads, README ×3, PRD)
- [x] Opt-in auto-config yields ready configs without manual curation (`DefaultCandidateEndpoints`, live: 4/4 networks in ~1.2 s)
- [x] Probing rejects empty-log endpoint with actionable message (unit tests + live publicnode demo; refusal shape also names the archive cause after round 1)
- [x] No new mandatory dependencies (csproj untouched; red-team verified)

### Fail-first evidence
- Feature tests: new-surface (cannot compile/pass pre-feature).
- Adversarial round-1 regression tests: 9 tests run against pre-fix commit `70e0f10` → **9 FAIL**; post-fix → 31/31 pass.

### Verification (post-fix)
Release build 0W/0E. Tests: Core 418, WebVh 423, Ethr 461, Key 52, Peer 48, DI 18, W3C 233 (green), Ethr integration 11+7 skipped. Samples 5/5 exit 0. `git diff --check` clean; w3c report churn restored.

### Behavior diff vs main
Purely additive (new public `EthrRpcAutoConfig`, `HistoricalLogProbe` internal; docs). No existing API or resolution behavior changed.

### Adversarial review round 1 (2 independent worktree-isolated agents)
CONFIRMED & fixed (9): probe-match validation (both agents; Medium), exception-object log injection (Medium/Low), refusal-shape actionability, CancelAfter ceiling, factory/Dispose containment, enumeration-based snapshot, immutable defaults, comment event-kinds, AddDidEthr XML docs.
Refuted/out-of-scope: hostile caller-dictionary enumerator throw (caller-owned, fail-loud); log-forging via caller-supplied keys/URLs (caller is the trust boundary; documented); sync-blocking preemption (documented limitation, seam-only).
**Verdict: clean** after round-1 fixes; re-verification green; live smoke re-confirmed (stricter probe still passes all 4 honest heads; publicnode refusal logs archive cause).

### Review round 2 (2026-08-04) — maintainer review comment 5168416278

All five findings addressed in commit `0cd8649`; 7 new fail-first regression tests (red pre-fix). PR reply: comment 5170056657.

| # | Severity | Finding | Resolution | Pinned by |
|---|---|---|---|---|
| 1 | HIGH | Mainnet probe (2020 window) approved endpoints pruned before block 10M; real events exist at 7,049,729 (2019-01-11) | Reviewer's evidence re-verified live + confirmed as the registry's earliest events via Blockscout; probe retargeted to block 7,049,729 / identity `0xee9b…cfa22`; all probes now target their network's earliest verifiable event | `Issue119_EndpointPrunedBefore10M_MainnetProbe_Rejected`; invariant pin of block+identity |
| 2 | MEDIUM | 41-block probe range could false-reject providers with tight range caps; resolution queries exact blocks | `HistoricalLogProbe` reshaped to a single `Block`; probe queries `fromBlock == toBlock` (resolution's own shape); all six verified live | `Issue119_ProbeFilter_…` pins `FromBlock == ToBlock == probe.Block` |
| 3 | HIGH | Raw RPC URLs (userinfo/path/query credentials) logged on every outcome incl. success | `SanitizeEndpoint`: scheme + host (+ non-default port) only; unparseable candidates logged by position only; returned configs keep full URL; PRD claim corrected | `Issue119_SecretUrl_SelectedEndpoint_…`, `Issue119_SecretUrl_EveryDiscardShape_…`, `Issue119_SecretInUnparseableCandidate_…` |
| 4 | — | Defaults covered 4 of 12 networks vs issue's "without manual curation" | Extended to 6 (added Aurora block 57,701,343 + EWC block 15,358,884, verified on official endpoints); claim explicitly narrowed in README/PRD — remaining 6 are deprecated/defunct (ARTIS, Mumbai, Linea Goerli) or lack verifiable probe data (Holesky, Volta) | `Issue119_EveryDefaultEndpointNetwork_HasProbeData` (now 6) |
| 5 | — | Snapshot materialized caller input before first ct check, unbounded | Token checked before/during snapshot; caps 64 networks / 16 candidates per network, truncation logged | `Issue119_PreCancelledToken_…`, `Issue119_CandidatesPerNetwork_Capped…`, `Issue119_NetworkEntries_Capped…` |

Post-fix verification: Release build 0W/0E; full suite green — Core 418, WebVh 423, **Ethr 486** (38 `Issue119_*`), Key 52, Peer 48, DI 20, W3C 233, integration 11+7 skipped; samples exit 0; `git diff --check` clean; live smoke: 6/6 default networks configured (~2 s) with earliest-event exact-block probes; publicnode discard path re-confirmed live.

### Review round 3 (2026-08-04) — maintainer review comment 5170135130

All six findings addressed in commit `e69ecc7`; 6 clean fail-first regressions (red in 119 ms pre-fix). PR reply: comment 5170463612. PR body rewritten.

| # | Severity | Finding | Resolution | Pinned by |
|---|---|---|---|---|
| 1 | MEDIUM | Cancel-then-throw from hostile snapshot enumerators escaped as the decoy exception, not OCE | Caller-token-priority catch around snapshot enumeration | `Issue119_OuterMapCancelsThenThrows_…`, `Issue119_InnerListCancelsThenThrows_…` |
| 2 | MEDIUM | Caller map keys logged raw/unbounded (CR/LF forging, flooding, URL-shaped credentials) | Ordinal-based identification for skipped entries; canonical `network.Name` after resolution | `Issue119_HostileMapKeys_NeverLoggedRaw`, `Issue119_UnknownNetworkKey_SkippedWithOrdinalLog` (inverted) |
| 3 | BLOCKING | Scope narrowed unilaterally in PRD; catalogue language contradictory; gap not test-visible | `NetworksWithoutDefaults` exclusion map with per-entry reasons; partition invariant test; `KnownNetworks` language reconciled; amendment proposed on issue #119 (comment 5170455250) for maintainer sign-off; follow-up issue #130 | `Issue119_EveryCatalogueNetwork_IsCoveredOrExplicitlyExcluded` |
| 4 | MEDIUM | "Same query shape as resolution" false — probe used wildcard topic0; resolver uses signature OR-list | Probe issues resolver's exact filter (topic0 OR-list + identity, exact block); returned topic0 validated; six live heads re-verified | `Issue119_ProbeFilter_…` (pins OR-list), foreign-topic0 shape in `Issue119_FabricatedLogs_…` |
| 5 | MEDIUM | NonTerminatingList "fail-first" evidence wedged/OOMed instead of failing on assertions | Throw-on-first-enumeration list (pre-cancel proof) + finite Max+2 sequence (caps); record corrected honestly in the PR reply | Reworked `Issue119_PreCancelledToken_…`, `Issue119_CandidatesPerNetwork_Capped…` |
| 6 | — | PR body and CHANGELOG stale (4 defaults, windows, 31 tests) | PR body rewritten; CHANGELOG wording fixed + rounds 2–3 recorded | — |

Post-fix verification: Release build 0W/0E; full suite green — Ethr **490** (42 `Issue119_*`), Core 418, WebVh 423, W3C 233, Key 52, Peer 48, DI 20, integration 11+7 skipped; samples exit 0; `git diff --check` clean; live smoke 6/6 default networks (~2 s) with resolver-shaped probes.
