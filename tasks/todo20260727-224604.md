# Issue #109 — Observe WaitAsync-abandoned RPC tasks (did:ethr)

## Context

PR #108's hardening wraps did:ethr RPC/signer awaits in `.WaitAsync(ct)` so a token-ignoring client cannot hang a write past `WriteDeadline`. That gain stays — but `WaitAsync` **abandons** the underlying task rather than cancelling it. If the abandoned task later faults, nothing observes the exception and the finalizer raises `TaskScheduler.UnobservedTaskException` in the host: telemetry noise for hosts that hook the event (App Insights, Sentry), and a process kill for hosts opting into `ThrowUnobservedTaskExceptions`. Measured, not theoretical (probe in the issue). Severity: **Low** (no correctness/security impact) — matches the issue.

## Assessment (validated against source)

Root cause confirmed. Every `.WaitAsync(ct)` on a dependency-produced task is an abandonment site. Full inventory (grep of `src/`):

| File | Sites |
|---|---|
| `src/NetDid.Method.Ethr/Transactions/TransactionPipeline.cs` | 163, 168, 179, 226 (signer), 257, 276 — 6 sites |
| `src/NetDid.Method.Ethr/DidEthrMethod.cs` | 506, 530, 547, 625, 717, 723, 768, 773 (signer) — 8 sites |
| `src/NetDid.Method.Ethr/Deployment/Erc1056Registry.cs` | 91 — 1 site (**not named in the issue**, same defect class; lessons.md: close the class, not the fixture) |

All 15 are `Task<T>`; no non-generic usages.

**Out of scope, with justification:**
- `Task.Delay(ReceiptPollInterval, ct)` (TransactionPipeline.cs:315) — `Task.Delay` observes its token natively; cancellation is real cancellation, nothing is abandoned.
- The resolver read path (`DidEthrResolver`) and all other DID methods — grep shows zero `WaitAsync` sites outside the three files above; their deadline handling passes `ct` directly, so faults are always observed by the awaiter.
- Changing the `WaitAsync` deadline semantics themselves — the issue explicitly says the robustness gain must stay. The deliberate "already-completed task wins over a canceled token" race handling (TransactionPipeline.cs:258 comment + explicit `ThrowIfCancellationRequested` re-checks) is preserved because the helper still returns `task.WaitAsync(ct)`.

## Fix (issue's suggested shape, as an extension method)

New internal extension, file `src/NetDid.Method.Ethr/AbandonableTaskExtensions.cs`, namespace `NetDid.Method.Ethr` (visible to the `Transactions`/`Deployment` child namespaces without new usings):

```csharp
internal static class AbandonableTaskExtensions
{
    public static Task<T> WaitAsyncObserved<T>(this Task<T> task, CancellationToken ct)
    {
        // WaitAsync abandons rather than cancels; observe a later fault so it cannot
        // surface as a TaskScheduler.UnobservedTaskException escalation in the host.
        _ = task.ContinueWith(
            static t => _ = t.Exception,
            CancellationToken.None,
            TaskContinuationOptions.OnlyOnFaulted | TaskContinuationOptions.ExecuteSynchronously,
            TaskScheduler.Default);
        return task.WaitAsync(ct);
    }
}
```

- `CancellationToken.None`: the observer itself can never be canceled away.
- `OnlyOnFaulted`: on success/cancel the continuation is *canceled*, and canceled tasks never raise `UnobservedTaskException`.
- `ExecuteSynchronously` + `TaskScheduler.Default`: no ambient-scheduler capture, negligible overhead (each site is a network round-trip anyway).

Then a mechanical swap at all 15 sites: `.WaitAsync(ct)` → `.WaitAsyncObserved(ct)` (respectively `token`). No behavior change on any non-fault path.

## Regression tests — `Issue109_*`, fail-first

New file `tests/NetDid.Method.Ethr.Tests/Issue109UnobservedTaskTests.cs`, reusing the `EmulatedEthereumChain` + delegating-RPC-client harness from `Issue107ReviewRound2Tests.cs` (`MethodFor`, `NewActor`, evidence accessors via `DidEthrMethod.LandedTransactionsKey`/`InFlightTransactionsKey`).

UnobservedTaskException is process-global and xUnit-parallel, so escalations are correlated via a dedicated marker exception type carrying a per-test GUID; the hook counts only matching escalations, after `GC.Collect` + `WaitForPendingFinalizers` + `GC.Collect`.

1. **Helper unit — abandonment observed**: `WaitAsyncObserved` on a `TaskCompletionSource<T>` task, token cancels → OCE; fault the TCS afterwards with the marker, drop refs, force finalization → **zero** marker escalations.
2. **Helper unit — behavior parity**: success returns value; pre-completion fault propagates the original exception; pending + canceled token → OCE; already-completed task + already-canceled token still returns the result (pins the TransactionPipeline.cs:258 semantics).
3. **End-to-end, TransactionPipeline receipt poll** (the issue's probe): `UpdateAsync` with a client whose `GetTransactionReceiptAsync` ignores `ct`, triggers caller cancellation, then faults ~300 ms after abandonment → operation fails as today (OCE, `InFlight` single, `Confirmed` empty — acceptance criterion 3), and zero marker escalations (criterion 1).
4. **End-to-end, DidEthrMethod site**: same shape through `GetChainIdAsync` ignoring the token (covers the `ExecuteOperationsAsync` pre-flight sites).
5. **End-to-end, Erc1056Registry.DeployAsync site**: `GetChainIdAsync` ignores token, faults after abandonment → zero escalations.
6. **Deadline unchanged (criterion 2)**: token-ignoring, never-completing receipt task + `WriteDeadline = 25 ms` → write still fails with the deadline `EthereumInteractionException` carrying the in-flight hash.
7. **Class guard**: a source-conformance test walking up to the repo root and asserting no bare `.WaitAsync(` call site exists under `src/NetDid.Method.Ethr/` outside the helper file — prevents the class from being reintroduced (skips cleanly if sources aren't present).

**Fail-first proof**: stash the src fix, run test 3 → must FAIL (marker escalation observed); restore, run → green. Recorded in the plan-file Review section.

## Steps

1. Branch `fix/issue-109-observe-abandoned-rpc-tasks` off `main`; copy this plan to `tasks/todo{timestamp}.md`.
2. Add `AbandonableTaskExtensions`; swap the 15 sites.
3. Add `Issue109UnobservedTaskTests`; prove fail-first by stashing the src change.
4. Verify via **net-did-verify**: 0-warning Release build, full suite with per-project counts (361 ethr baseline + new), W3C conformance green, samples end-to-end, `git diff --check`. Run the 7 Anvil integration tests if Docker is available (Testcontainers); report if not.
5. **adversarial-review** on the diff (mandatory); fix anything CONFIRMED, re-verify.
6. Docs: PRD §write-path item 3 (~line 1493) gains one sentence: abandoned awaits are fault-observed so they can never surface as `UnobservedTaskException`. CHANGELOG `[Unreleased]` → `Fixed`. No README change (no public behavior change). **No version bump.**
7. Commit, push, open PR with `Fixes #109`; append `## Review` to the plan file. **Stop at the open PR.**

## Files touched

| File | Change |
|---|---|
| `src/NetDid.Method.Ethr/AbandonableTaskExtensions.cs` | new — the observing helper |
| `src/NetDid.Method.Ethr/DidEthrMethod.cs` | 8 call-site swaps |
| `src/NetDid.Method.Ethr/Transactions/TransactionPipeline.cs` | 6 call-site swaps |
| `src/NetDid.Method.Ethr/Deployment/Erc1056Registry.cs` | 1 call-site swap |
| `tests/NetDid.Method.Ethr.Tests/Issue109UnobservedTaskTests.cs` | new — tests 1–7 |
| `NetDidPRD.md`, `CHANGELOG.md`, `tasks/todo{timestamp}.md` | docs + tracking |

## Review

**Branch:** `fix/issue-109-observe-abandoned-rpc-tasks` — commits `1c509b8` (fix + tests), `f58e7a6` (docs, committed by Moises), `57a1483` (adversarial-review fixes).

**Files changed:** `src/NetDid.Method.Ethr/AbandonableTaskExtensions.cs` (new), `DidEthrMethod.cs` (8 swaps), `Transactions/TransactionPipeline.cs` (6 swaps), `Deployment/Erc1056Registry.cs` (1 swap), `tests/NetDid.Method.Ethr.Tests/Issue109UnobservedTaskTests.cs` (new, 8 tests), `NetDidPRD.md`, `CHANGELOG.md`.

**Fail-first evidence:** with the three src files reverted to `main` (helper kept so the suite compiles), 4/7 Issue109 tests FAIL — the three e2e escalation tests each observe exactly one marker escalation, and the source guard names all 15 bare `.WaitAsync(` sites. Restored → 7/7 (later 8/8) green. The retention test was separately mutation-verified: un-deduped observer retains 50,086,832 B over 200k attempts vs the 10 MB bound (KILLED).

**Verification (run twice — after the fix and after the adversarial rework):** Release build 0 warnings / 0 errors. Tests: Core 377, Key 52, Peer 48, WebVh 420, Ethr 369 (361 baseline + 8 new), DI 18, W3C conformance 233 — all green. Anvil integration (Docker, real EVM): 7/7. Samples: all five exit 0. `git diff --check` clean.

**Acceptance criteria (issue #109):** zero UnobservedTaskException escalations on abandoned-then-faulting paths (3 e2e tests + helper unit test, GC + WaitForPendingFinalizers probe, marker-correlated) ✔; deadline/cancellation unchanged (parity test incl. the completed-task-beats-canceled-token race; token-ignoring hanging receipt still bounded by WriteDeadline) ✔; evidence contract unchanged (Landed/InFlight keys asserted on abandonment + deadline paths) ✔; 361 existing + 7 Anvil green ✔.

**Adversarial review (3 independent worktree-isolated agents + 1 re-attack):**
- TPL-correctness lens: all angles refuted with probe evidence (12 probes); mutation check — reverting the helper fails 5 tests. One Low informational (WaitAsync proxy task itself unobserved if a future site composes without awaiting — parity with pre-fix, all 15 sites await immediately).
- Test-validity lens: five helper mutations all KILLED by exactly the right tests; 20/20 stability runs; F1 (Low) guard-disarms-silently-out-of-tree → fixed (fails loudly when `CI` is set); F2–F4 informational, accepted.
- Hostile-dependency lens: CONFIRMED (Low) — permanent observer accumulation on a shared forever-pending hostile task (~271 B/attempt, unbounded) → fixed in `57a1483` via ConditionalWeakTable dedupe (≤1 observer per task instance) + `!ct.CanBeCanceled` skip; pinned by the 200k-iteration retention test. All other angles (hostile Task<T> subclass, ExecuteSynchronously injection, fault suppression, evidence divergence — 8-scenario differential fuzz + 7 hostile client shapes) refuted.
- Re-attack round on the dedupe surface: launched; verdict to be posted on the PR.

**Verdict: clean** (all CONFIRMED findings fixed and re-verified).

**Behavior diff vs `main`:** none on any success, deadline, cancellation, or evidence path (probe-verified identical task instances and exception identity); the only change is that an abandoned dependency task's later fault is observed instead of escalating to the host.
