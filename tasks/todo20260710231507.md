# Address GitHub issues #87 and #88

- [x] Confirm the current `main` baseline, create one feature branch for both issues, and capture focused baseline test results.
- [x] Add failing regressions for issue #87 covering equal and decreasing adjacent `versionTime` values, fractional-second increases, valid whole-second increases, resolver `invalidDidLog` mapping, and Update/Deactivate rejection of caller-supplied non-monotonic logs.
- [x] Enforce a strictly increasing authenticated `versionTime` invariant in the shared log-chain validator without normalizing or rewriting the exact wire timestamp token.
- [x] Add failing regressions for issue #88 covering duplicate witness IDs, zero and out-of-range thresholds, malformed genesis and later policy transitions, legacy weights, duplicate proof votes, and valid distinct-signer threshold coverage.
- [x] Centralize witness-policy shape validation at the parameter-validation boundary, apply it to genesis and every later declared policy, and count each distinct cryptographically verified witness ID as one approval regardless of legacy `Weight` values.
- [x] Preserve source/wire compatibility for `WitnessEntry.Weight` while documenting that did:webvh 1.0 ignores it; document migration from weighted policies to distinct-witness-count thresholds.
- [x] Update `NetDidPRD.md` to make monotonic time, witness-policy validation, transition timing, compatibility, and distinct-signer counting normative for this implementation.
- [x] Run focused and full did:webvh tests, the complete solution test suite, release build, and formatting/diff checks; compare behavior against `main`.
- [x] Run an independent adversarial review of timestamp, policy-transition, duplicate-identity, malformed-key, and proof-counting edge cases; address findings and repeat affected validation.
- [ ] After successful validation, update `CHANGELOG.md`, add exact evidence to the review section below, commit/push the single branch, and open one PR that closes both #87 and #88.

## Review results

- Baseline: current `main` (`42b721b`) passed the original did:webvh suite, 249/249.
- Final focused timestamp/security set: 28/28 passed after adversarial fixes. The broader
  timestamp, witness-policy, witness-proof, and serializer set passed 58/58 before the final
  metadata regression was added.
- Final did:webvh project: 282/282 passed.
- Final complete solution: 927/927 passed (`dotnet test netdid.sln --no-restore --tl:off
  --disable-build-servers -m:1`).
- Final release build: succeeded with 0 warnings and 0 errors (`dotnet build netdid.sln -c
  Release --no-restore --tl:off --disable-build-servers -m:1`).
- Two independent adversarial reviews exercised authenticated equal/decreasing timestamps,
  malformed timestamp tokens, historical selection/metadata, future/max-time writing, witness
  shape/transition rules, Unicode identity normalization, null entries, key types, duplicate
  proofs, legacy weights, and authorization equality. Findings prompted fixes for malformed-token
  error mapping, non-monotonic historical scanning, unvalidated-tail metadata leakage, NFC identity
  confusion, null programmatic entries, and stale source-of-truth documentation. Final re-reviews
  found no remaining implementation/security defects.
- `git diff --check` passed and build-generated `w3c-conformance-report.md` drift was restored.
  `dotnet format ... --verify-no-changes` could not load its Roslyn build-host named pipe and timed
  out twice; the final compiler/build validation remained clean at zero warnings.
