# Fix did:webvh v1.0 pre-rotation authorization (#93)

## Plan

- [x] Confirm the issue against the normative did:webvh v1.0 rules and map every affected writer, resolver, public API, test, and documentation path.
- [x] Replace the legacy `prerotation`-flag model with v1.0 `nextKeyHashes`-driven activation; stop emitting the removed wire parameter and reject it in v1.0 logs rather than silently accepting a pre-v1.0 contract.
- [x] Make Update select authorization by the prior entry's pre-rotation state: use the current entry's explicit committed `updateKeys` while active, otherwise use the prior effective `updateKeys`.
- [x] Enforce active pre-rotation entry shape and commitments: explicit non-empty `updateKeys`, explicit `nextKeyHashes` (including `[]` for the deactivating transition), and hash membership for every current key.
- [x] Make chain validation apply the same current-vs-prior key selection and validate required parameter presence before proof authorization.
- [x] Make Deactivate produce a conforming entry while pre-rotation is active by revealing/signing with a committed key and explicitly ending pre-rotation; retain the ordinary prior-key path otherwise.
- [x] Reconcile #91 authorization evidence and documentation with the corrected model without claiming future signer keys that cannot be derived from hashes.
- [x] Add positive and negative regression tests covering writer/resolver interop semantics, previous-key rejection, committed current-key acceptance, explicit-field requirements, activation/deactivation transitions, legacy-flag rejection, and deactivation.
- [x] Run focused tests, the full solution test suite, formatting/static checks available in the repository, and inspect the final diff.
- [x] Run an independent adversarial review of the completed patch and resolve all actionable findings.
- [x] Update `NetDidPRD.md`, README/API XML documentation, and `CHANGELOG.md`; record exact validation evidence below.

## Review results

- Implemented on `fix/issue-93-webvh-prerotation-conformance` from main commit `5788da4`.
- Writer and resolver now implement the same v1.0 authorization state machine: ordinary entries use prior effective keys; entries governed by prior non-empty commitments use current explicit committed keys.
- Removed the legacy public/wire `prerotation` switches; `nextKeyHashes` alone activates and ends pre-rotation.
- Active entries require explicit `updateKeys` and `nextKeyHashes`; the entire current key set is commitment-checked and validated as Ed25519 Multikeys.
- Active deactivation reveals/signs with a committed key, emits `nextKeyHashes: []`, and resolves with no DID Document. Valid historical versions report a later fully verified deactivation without trusting an identity-mismatched tail.
- Unsupported method versions and unknown log/parameter properties fail closed. The defined v1.0 `watchers` parameter is preserved and validated as non-null absolute HTTP(S) URLs.
- #91 evidence remains `Unknown` / `null` only while the resulting state keeps non-empty commitments; ending pre-rotation restores concrete next-entry key evidence.
- Focused final conformance/adversarial filter: 25/25 passed.
- Final full solution test run: 970/970 passed (`323` webvh, `374` core, `175` W3C, `44` did:key, `40` did:peer, `14` DI).
- `git diff --check` passed.
- A redundant standalone `dotnet build netdid.sln` attempt first stalled under sandboxed MSBuild IPC and its escalated retry was denied by the environment approval quota. The final `dotnet test netdid.sln --no-restore --disable-build-servers` completed successfully and compiled all tested solution dependencies.
- Independent test-gap and adversarial reviews were completed. All actionable findings were fixed; final adversarial verdict: clean, with no remaining #93 blocker.
- Preserved the user's pre-existing `w3c-conformance-report.md` modification and unrelated untracked task files.
