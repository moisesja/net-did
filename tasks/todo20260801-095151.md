# Issue #116 — did:ethr: RPC infrastructure failures resolve as `notFound` instead of `internalError`

## Context

`DidEthrMethod.ResolveCoreAsync` fails closed correctly (no truncated document is ever returned), but its catch-all ([DidEthrMethod.cs:160-164](src/NetDid.Method.Ethr/DidEthrMethod.cs#L160-L164)) converts **every** failure inside the chain-walk — pruned/hostile-node detection (`EthereumInteractionException`), raw transport errors (`HttpRequestException` escapes `DefaultEthereumRpcClient.SendAllowingNullResultAsync` unwrapped), internal deadline/per-request timeouts (`OperationCanceledException` where the caller's token is NOT canceled), and any parse escape — into `notFound`.

Per the W3C DID Resolution spec (verified against https://www.w3.org/TR/did-resolution/): `NOT_FOUND` means *the input DID does not exist*; unexpected resolver errors "MUST return" `INTERNAL_ERROR`. The library (and the reference `ethr-did-resolver` ecosystem) uses the camelCase registry vocabulary (`notFound`, `invalidDid`, …), so the correct code here is **`internalError`** — exactly what the issue requests. Severity: **Medium-High** (callers treat `notFound` as authoritative "DID never existed" and may reject credentials/purge caches when the correct reaction is "retry against an archive node").

Note: a genuinely unregistered did:ethr identity never errors at all — `changed() == 0` yields the ERC-1056 genesis document. So *every* failure inside the RPC-consuming `try` block is infrastructure, never a statement about the DID's existence.

### Out of scope (with justification)
- **`FindNetwork` failure → `notFound`** ([DidEthrMethod.cs:129-135](src/NetDid.Method.Ethr/DidEthrMethod.cs#L129-L135)): unconfigured network is resolver *configuration*, not an RPC infrastructure failure; the reference resolver uses a distinct `unknownNetwork` code. Changing it belongs to the #120 interop work — will be noted in the PR body, not changed here.
- **Identifier parsing → `invalidDid`**, **options parsing → `invalidOptions`**: already spec-correct; issue confirms.
- **Update/Deactivate read-back** ([DidEthrMethod.cs:642-649](src/NetDid.Method.Ethr/DidEthrMethod.cs#L642)): already guards on *any* non-null `Error`, so it is robust to the new code; only its comment wording ("maps … to a NotFound RESULT") needs a touch-up.
- **Other methods (key/peer/webvh)**: no shared code path; their error mapping is untouched.

## Plan

1. **Branch** `fix/issue-116-rpc-internal-error` off up-to-date `main`; plan file `tasks/todo{timestamp}.md`.

2. **Core factory** — [src/NetDid.Core/Model/DidResolutionResult.cs](src/NetDid.Core/Model/DidResolutionResult.cs): add
   ```csharp
   public static DidResolutionResult InternalError(string did, string? reason = null)
   ```
   `Error = "internalError"`; when `reason` is non-null, `AdditionalProperties = { ["message"] = reason }` (the `message`-beside-`error` shape matches the reference `ethr-did-resolver`'s resolution metadata, which #120 will fixture against).

3. **Fail-first regression tests** (written BEFORE the src change; they compile pre-fix because they assert on the string `"internalError"`, and fail with `notFound`) — new file `tests/NetDid.Method.Ethr.Tests/Issue116ResolutionErrorMappingTests.cs`, `Issue116_*` names:
   - Empty logs for a block asserted by `changed()` → `internalError` (the issue's canonical repro).
   - `GetLogsAsync` throws `HttpRequestException` (transport) → `internalError`.
   - `CallAsync` throws `HttpRequestException` → `internalError`.
   - Internal deadline/timeout: RPC mock throws `OperationCanceledException` on a fresh token (caller's token not canceled) → `internalError`, not a throw.
   - Caller cancellation still **propagates** as `OperationCanceledException` (never mapped).
   - Unregistered identity + healthy node (`changed()` → 0) → genesis document, `Error == null` (acceptance criterion).
   - Invalid identifier → `invalidDid` unchanged (pin).
   - Metadata carries `AdditionalProperties["message"]` distinguishing the pruned-node reason.
   - Hostile `EthereumInteractionException` **subclass** whose `Message` getter throws → still returns `internalError` with the fixed fallback reason (lesson: dependency exceptions are untrusted, `Message` is virtual).

4. **Fix** — [src/NetDid.Method.Ethr/DidEthrMethod.cs](src/NetDid.Method.Ethr/DidEthrMethod.cs#L150-L165): replace the catch-all with:
   - `catch (OperationCanceledException) when (ct.IsCancellationRequested)` → rethrow (unchanged);
   - `catch (OperationCanceledException)` → `InternalError(did, <fixed text: resolution deadline/RPC timeout>)`;
   - `catch (EthereumInteractionException ex)` → `InternalError(did, reason)` where `reason = ex.Message` **only if `ex.GetType() == typeof(EthereumInteractionException)`** (exact known-safe type, per lessons), truncated to a bounded length (RPC `error` JSON is interpolated into some messages and is attacker-sized); otherwise fixed text;
   - `catch (Exception ex)` → `InternalError(did, <fixed text>)`.
   - Update the lines 138-147 comment ("Map any such failure to notFound" → internalError) and the line-642 comment.

5. **Update outdated proxies** — flip `notFound` → `internalError` assertions on infrastructure paths (the fail-closed property is preserved and made spec-precise):
   - `DidEthrResolverHardeningTests.cs`: hop-cap, event-count-cap, byte-cap, malformed/non-canonical `changed()` results, `InvalidOperationException` escape (~7 sites incl. method names saying `ReturnsNotFound`).
   - `DidEthrResolveCorrectnessTests.cs`: incomplete-history block (~10 sites: empty block, foreign identity/registry, forward previousChange, timestamp regression, duplicate logIndex, null logs, …).
   - `ReviewRound1Tests.cs`: comment only.
   - `invalidDid` assertions untouched.

6. **Verify** — `net-did-verify` skill: 0-warning Release build, full suite per-project counts, W3C conformance green (its `notFound` sites are dereferencing-only, unaffected), samples end-to-end.

7. **Adversarial review** — `adversarial-review` skill on the diff (error-path change on a trust boundary), fix CONFIRMED findings, re-verify.

8. **Docs + CHANGELOG** — PRD's did:ethr resolution/error-mapping section; README if it enumerates error codes; CHANGELOG `[Unreleased]` → `Fixed` with explicit behavior-change note (callers previously matching on `notFound` for infra failures must now match `internalError`). **No `NetDidVersion` bump.**

9. **PR** — `Fixes #116`, behavior diff vs main + fail-first evidence in body; append `## Review` to the plan file. **Stop at the open PR** for external review/merge.

## Verification
- New `Issue116_*` tests red on pre-fix source (step 3 run before step 4), green after.
- Full `dotnet test` per-project counts vs main; `dotnet build -c Release` 0 warnings; W3C conformance suite; samples.
- Confirm no remaining `notFound` returns from the RPC-consuming region: `grep -n NotFound src/NetDid.Method.Ethr/DidEthrMethod.cs` shows only the out-of-scope `FindNetwork` site.

## Review

**Files changed** (vs `main`):
| File | Change |
|---|---|
| `src/NetDid.Core/Model/DidResolutionResult.cs` | new `InternalError(did, reason)` factory (`error="internalError"`, optional `AdditionalProperties["message"]`) |
| `src/NetDid.Method.Ethr/DidEthrMethod.cs` | catch restructure in `ResolveCoreAsync` (OCE-caller rethrow / OCE-internal / `EthereumInteractionException` / catch-all → `InternalError`); `GetOrCreate` moved inside the guarded `try`; `LogResolveFailure` (never-throw under eager loggers); `SanitizeReason` (control-char strip, 500-char cap, surrogate-safe); comments updated |
| `tests/NetDid.Method.Ethr.Tests/Issue116ResolutionErrorMappingTests.cs` | 15 new `Issue116_*` tests |
| `tests/.../DidEthrResolverHardeningTests.cs`, `DidEthrResolveCorrectnessTests.cs`, `ReviewRound1Tests.cs` | `notFound`→`internalError` assertion/name flips on infra paths (`invalidDid` pins untouched) |
| `README.md`, `NetDidPRD.md`, `samples/NetDid.Samples.DidEthr/Program.cs`, `CHANGELOG.md` | behavior docs updated; `[Unreleased]` Fixed entry with explicit behavior-change note |

**Fail-first evidence**: `Issue116_*` run before the src change: 6 FAILED (actual `notFound`), 3 pins passed (caller cancellation, genesis doc, invalidDid). After the fix: 15/15 pass.

**Verification** (net-did-verify, re-run after adversarial fixes): Release build 0 warnings/0 errors; tests — Core 377, Key 52, Peer 48, WebVh 420, **Ethr 397** (382 baseline + 15 new), DI 18, W3C conformance 233 (green), Ethr integration 11 passed / 7 Docker-gated skips (write path untouched); all 5 samples exit 0 (DidEthr sample now prints `internalError` for incomplete history); `git diff --check` clean.

**Adversarial review** (2 worktree-isolated agents: trust-boundary + spec/completeness):
- CONFIRMED High — `LogWarning(ex, …)` let a hostile exception (throwing `Message`/`ToString`) escape `ResolveAsync` under a real eagerly-formatting logger. Fixed: `LogResolveFailure` guards the log call, falls back to type-name-only diagnostics; pinned by `Issue116_HostileException_EagerFormattingLogger_DoesNotEscapeResolveAsync`.
- CONFIRMED Low — `_rpcFactory.GetOrCreate` outside the `try` escaped on a throwing factory. Fixed: moved inside; pinned by `Issue116_RpcClientFactoryThrows_ReturnsInternalError`.
- CONFIRMED Medium/Low — node-controlled text in `message` (control chars, surrogate-splitting truncation). Fixed: `SanitizeReason`; pinned by the sanitization tests. Residual (documented in README/CHANGELOG): `message` remains untrusted text; consumers must escape it.
- CONFIRMED Low (test gaps) — chainId-RPC-fallback and versionId-block-timestamp paths unpinned. Fixed: two new `Issue116_*` tests.
- Deferred (documented scope cut, carried to PR body): `FindNetwork` unconfigured-network still `notFound` (reference resolver uses `unknownNetwork`) → #120 interop work. Theoretical (no change): OCE-filter reads token state, not the exception's token — only affects non-default RPC clients surfacing cancellation as custom types.
- Verdict after fixes: clean (core mapping + never-throw contract hold on all demonstrated paths; affected validation re-run green).

**Behavior diff vs main**: every failure inside the RPC-consuming region of did:ethr resolution returns `internalError` (+`message`) instead of `notFound`; `notFound` remains only on the unconfigured-network path; unregistered identities, `invalidDid`, `invalidOptions`, caller cancellation unchanged.

## Review round 1 (2026-08-01, PR #122 review 4834832484)

| # | Severity | Finding | Resolution | Pinning test |
|---|---|---|---|---|
| 1 | High | `LogResolveFailure`'s fallback log call was unguarded — an always-throwing logging provider escaped `ResolveAsync`; parse/network warning sites had the same exposure | Fallback wrapped in its own try + final swallow ("logging is best-effort"); pre-RPC warning sites (identifier parse, network lookup) individually guarded | `Issue116_AlwaysThrowingLogger_FactoryThrow_StillReturnsInternalError`, `..._InvalidIdentifier_StillReturnsInvalidDid`, `..._UnconfiguredNetwork_StillReturnsResult` |
| 2 | Medium | Exact runtime type is not provenance: `EthereumInteractionException` has a public ctor, so injected clients can throw the exact type with arbitrary/secret text which was published to `metadata.message`; raw multi-MB node error text also hit logs | Metadata reason is now ALWAYS fixed library-owned text; the one distinguishable category (pruned/incomplete history) is identified by new `internal sealed IncompleteEventHistoryException` (type-as-provenance — uninstantiable outside the assembly); `SanitizeReason` deleted; node-authored JSON-RPC error text bounded to 1 KiB at the source (`DefaultEthereumRpcClient`) | `Issue116_ExactTypeExceptionMessage_NeverPublishedToMetadata`; pruned-message test now pins the fixed text |
| 3 | High (docs) | "Per the W3C DID Resolution spec" claim false vs current draft (RFC 9457 error objects, `https://www.w3.org/ns/did#INTERNAL_ERROR`, empty didDocumentMetadata); conformance suite pins the legacy string | Reframed README/PRD/CHANGELOG/XML docs as legacy DID Spec Registries string vocabulary (reference-resolver-compatible), semantics per DID Resolution; filed **issue #123** tracking the library-wide RFC 9457 error-object migration | n/a (docs) |
| 4 | Low | `char.IsControl` misses U+2028/U+2029 (single-line claim false); literal NUL byte in test source | Both moot by design change: sanitization removed entirely (fixed strings only); test region rewritten, 0 NUL bytes verified | n/a |
| 5 | Pre-existing | 120 s deadline only passed a token — a cancellation-ignoring, never-completing injected client hung resolution forever | Deadline now binds the returned task via the project's `WaitAsyncObserved` (#109 mechanism); `ResolutionDeadline` made internal-settable (mirrors `WriteDeadline`); #109 lexical inventory updated 8→9 with rationale comment | `Issue116_CancellationIgnoringRpcClient_BoundedByResolutionDeadline` |

Re-verified after fixes: Release build 0 warnings; Core 377, Key 52, Peer 48, WebVh 420, **Ethr 400**, DI 18, W3C 233, integration 11+7 skipped; DidEthr sample exit 0; diff-check clean.

## Review round 2 (2026-08-01, PR #122 review 4834888386)

| # | Severity | Finding | Resolution | Pinning test |
|---|---|---|---|---|
| 1 | High | Deadline wrapped only the OUTER ResolveFromChainAsync task; the actual dependency awaits (CallAsync :253, GetBlockTimestampAsync :292/:304, GetLogsAsync :394, GetChainIdAsync :1191) stayed bare — a shared forever-pending task from a cancellation-ignoring client retained one inner continuation per resolution (reviewer repro: 100), and late completion resumed all abandoned machines, fanning out post-deadline GetLogsAsync calls | Every read-path dependency await is now individually `WaitAsyncObserved(ct)`-bounded (the write path already was): BCL WaitAsync removes its continuation on cancellation and the fault observer is deduped per source task, so a shared hung task retains ≤1 continuation and late completion resumes nothing; outer wrap kept as defense in depth; #109 lexical inventory 9→14 with rationale | `Issue116_SharedHungRpcTask_NoPerCallRetention_NoLateRpcFanout` (20 concurrent resolutions vs one shared TCS, 100 ms deadline → all internalError, continuation slot ≤1 via the #109 reflection pin, late completion → 0 GetLogsAsync calls) |
| 2 | Medium | `error.ToJsonString()` materialized the full attacker-controlled node before slicing — default-encoder escaping amplifies ~6x, so a near-16 MiB error could transiently allocate ~192 MiB | New `DescribeRpcError`: never serializes the node; reads only the JSON-RPC 2.0 scalar members — numeric `code` and ≤256-char prefix of string `message` (the only transient allocation is the string the parsed DOM already holds); non-object errors get fixed text | `Issue116_OversizedHighlyEscapableRpcError_MessageBoundedWithoutSerializingNode` (1M '&' message + 1M nested decoy → exception message <600 chars, contains code), `Issue116_NonObjectRpcError_YieldsFixedDiagnostic`; tiny-error test tightened to pin `code -32000` + `boom` |
| 3 | Low | PR body stale (sanitized-text claim, 15/397 counts, old conformance framing); deadline test only proved 30 s; pruned-message comment claimed block-specific diagnostic | PR description rewritten to match head (fixed messages, 19 Issue116 / 403 Ethr, legacy-vocabulary framing + #123); deadline test asserts elapsed <10 s vs the 250 ms config; comment corrected to fixed-category text | updated assertions in `Issue116_CancellationIgnoringRpcClient_BoundedByResolutionDeadline` |

Re-verified: Release build 0 warnings; Core 377, Key 52, Peer 48, WebVh 420, **Ethr 403**, DI 18, W3C 233, integration 11+7 skipped; DidEthr sample exit 0; diff-check clean (two consecutive full-suite runs green).

## Review round 3 (2026-08-01, PR #122 review 4834941775)

| # | Severity | Finding | Resolution | Pinning test |
|---|---|---|---|---|
| 1 | High | Already-COMPLETED tasks bypass the deadline: `WaitAsyncObserved` deliberately lets a completed task beat a fired token, and no loop re-checked `ct` — a token-ignoring client returning completed tasks ran the entire 1,000-hop walk after cancellation (reviewer repro: 1,000/1,000 post-cancel calls) | `ct.ThrowIfCancellationRequested()` added before EVERY dependency call: chainId + changed() (pre-call), per-hop in the walker loop, per-iteration in the versionTime timestamp loop, the versionId timestamp fetch, and the GetChainIdAsync fallback — the loops now observe cancellation on the synchronous fast path | `Issue116_CompletedTaskFastPath_CallerCancellation_StopsTheWalkImmediately` (cancel during hop 1 ⇒ OCE propagates, exactly 1 GetLogsAsync) and `..._DeadlineExpiry_StopsTheWalkImmediately` (first hop synchronously outlives a 50 ms deadline ⇒ internalError, exactly 1 call, not 10,000) |
| 2 | Medium | `TryGetValue<string>` materialized the full attacker message (~16 MB → ~32 MB LOH) before `[..256]`; decoded CR/LF/ESC/U+2028 reached exception/logs verbatim; `[..256]` could split a surrogate pair; duplicate members inside the error object threw raw `ArgumentException` from lazy JsonObject materialization (regression vs the serialize path) | Reviewer's "use a fixed diagnostic" option taken: `DescribeRpcError` reads ONLY the numeric JSON-RPC `code` — the message member is never read, so no attacker content of any size/shape can reach exceptions or logs (structural, not a cap). JSON-access failures caught inside `DescribeRpcError` → "malformed error member"; envelope member access (`responseObject["error"]/["result"]`) guarded → "invalid or duplicate envelope members" | `Issue116_OversizedRpcErrorMessage_NeverMaterialized` (1M-char message+decoy ⇒ <200-char diagnostic, no `&`), `Issue116_LogForgingRpcErrorMessage_NoHostileContentInDiagnostic` (CR/LF/ESC/U+2028/astral via JSON escapes ⇒ none present, no surrogates), `Issue116_DuplicateMembersInsideErrorObject_MappedAtTrustBoundary`, `Issue116_DuplicateEnvelopeMembers_MappedAtTrustBoundary`; tiny-error pin now also asserts the message text ("boom") is ABSENT |

Trade-off noted: the node's error message text no longer appears anywhere (code number only) — bounded-by-construction beats bounded-by-cap after three rounds; operators needing message text can capture wire traffic. Re-verified: build 0 warnings; Core 377, Key 52, Peer 48, WebVh 420, **Ethr 408**, DI 18, W3C 233; integration 11+7 skipped; sample exit 0; diff-check clean.

## Review round 4 (2026-08-01, PR #122 review 4834987800)

| # | Severity | Finding | Resolution | Pinning test |
|---|---|---|---|---|
| 1 | High | Pre-call-only guards left the TERMINAL path open: cancellation/deadline expiry during the LAST completed dependency call fell through to a successful return (reviewer repros: deadline-expired terminal hop returned a document at 330 ms past a 50 ms deadline; caller-cancelled changed()=0 returned a genesis doc), and `.ToList()` on a hostile nonterminating IReadOnlyList hung synchronously | Symmetric guards: `ct.ThrowIfCancellationRequested()` AFTER every dependency await (chainId, changed(), walker per-hop, both timestamp paths) before any parsing; dependency-owned log collections materialized by our own bounded loop (per-item token check + MaxCollectedEvents cap) instead of bare `.ToList()`; a final token gate immediately before the success return. Contract honestly narrowed in the `ResolutionDeadline` remarks: token bounds cannot preempt SYNCHRONOUS blocking inside an injected client method or a hostile MoveNext — hard bound holds against hostile nodes via the fully-async `DefaultEthereumRpcClient`; untrusted in-process client implementations need process isolation | 7 new tests, ALL proven red pre-fix (6 fail, the nonterminating one HANGS pre-fix — demonstrated by a timed-out run): `Issue116_ChangedZero_CallerCancelledDuringCompletedCall_NoSuccessReturned`, `Issue116_ChangedZero_DeadlineExpiredDuringCompletedCall_InternalError`, `Issue116_TerminalHop_SlowEnumerationPastDeadline_InternalError_NotSuccess` (reviewer's exact repro, with elapsed bound), `Issue116_TerminalHop_CallerCancelledDuringFinalGetLogs_Propagates`, `Issue116_NonterminatingLogEnumeration_BoundedByCount_InternalError`, `Issue116_FinalVersionIdTimestamp_DeadlineExpired_InternalError`, `Issue116_FinalVersionTimeTimestamp_CallerCancelled_Propagates` |
| 2 | Low | PR description stale again (≤256-prefix claim, old counts, "two review rounds") | Description rewritten from head: code-only diagnostic, 28 method + 5 RPC Issue116 tests, Ethr 415, four review rounds | n/a |

Re-verified: build 0 warnings; Core 377, Key 52, Peer 48, WebVh 420, **Ethr 415**, DI 18, W3C 233; integration 11+7 skipped; sample exit 0; diff-check clean.
