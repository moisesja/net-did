# Review newly opened pull request

- [x] Identify the newly opened PR and capture its title, description, commits, checks, and complete diff.
- [x] Review changed critical paths, scope, tests, and commit quality against repository conventions.
- [x] Run targeted validation where it materially strengthens the review.
- [x] Post a specific GitHub review comment or approval.

## Review

Reviewed PR #108 (`feat(ethr): on-chain write path — Update, Deactivate,
meta-transactions, registry deployment`), a cohesive but large security-critical change
(49 files, +6,854/-581). CI was green. Independently ran the Release
`NetDid.Method.Ethr.Tests` project: 326/326 passed.

Posted a blocking GitHub review with five reachable findings:

1. Update/Deactivate ignore configured `MaxTransactionFeeWei`.
2. `BroadcastTransactionKey` is never folded into landed-transaction evidence.
3. Registry deployment lets the untrusted RPC choose the signing chain ID.
4. Meta-transaction controller signatures are not verified before relayer spend.
5. Deactivation maps post-write resolution failure to a false success result instead of
   reporting unknown state with landed-transaction evidence.

The connected GitHub app lacked review-write permission (HTTP 403), so the authenticated
`gh pr review` fallback was used. The posted review was verified in PR metadata with state
`COMMENTED` at `2026-07-27T03:24:46Z`.
