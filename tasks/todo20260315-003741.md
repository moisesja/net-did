# Follow-up Review: Revalidate Issues #26, #29, #15, and #17

## Context

- Date: 2026-03-15
- Scope: verify whether issues `#26`, `#29`, `#15`, and `#17` are actually fixed in the current codebase
- Focus:
  - current implementation
  - regression test coverage
  - current GitHub issue state
- Note: `tasks/lessons.md` is still not present in this repository

## Checklist

- [x] Create a dedicated revalidation note
- [x] Inspect current implementation for `#26`, `#29`, `#15`, and `#17`
- [x] Inspect relevant regression tests
- [x] Check current GitHub issue state
- [x] Reopen/comment on any issues whose state no longer matches reality
- [x] Record final classification and actions

## Working Notes

- `#26` is fixed. `DidManager` and `CompositeDidResolver` now validate DID syntax up front and return `invalidDid`, with direct regression coverage.
- `#17` is fixed. The did:webvh create/update/deactivate APIs now accept witness proof input and can emit `did-witness.json`, with end-to-end artifact tests.
- `#15` is only partially fixed. Missing/malformed witness files and array parsing were fixed, but witness validation still requires an exact `versionId` match for each witnessed entry instead of allowing later witness entries to satisfy earlier versions.
- `#29` is only partially fixed. `serviceType` and URI construction improved, but default service dereferencing still returns a `Service` object, service lookup still matches by fragment only, and service endpoint sets remain unhandled.
- Targeted verification passed:
  - `dotnet test tests/NetDid.Core.Tests/NetDid.Core.Tests.csproj --no-restore` -> `287 passed`
  - `dotnet test tests/NetDid.Method.WebVh.Tests/NetDid.Method.WebVh.Tests.csproj --no-restore` -> `69 passed`

## Findings

- Leave closed:
  - `#26`
  - `#17`
- Reopen with detailed follow-up:
  - `#15`
  - `#29`

## Actions Taken

- Reopened `#15` and added follow-up comment:
  - `https://github.com/moisesja/net-did/issues/15#issuecomment-4064386763`
- Reopened `#29` and added follow-up comment:
  - `https://github.com/moisesja/net-did/issues/29#issuecomment-4064386760`
