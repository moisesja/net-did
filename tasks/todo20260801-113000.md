# Issue #117 — did:ethr didDocumentMetadata `updated` / `nextUpdate`

## Context

The reference `ethr-did-resolver` populates `didDocumentMetadata.updated` (ISO 8601 UTC
block timestamp of the last applied change) on every resolution with history, and
`nextUpdate` alongside `nextVersionId` on historical (`versionId`/`versionTime`) queries.
NetDid emits neither, so the maintainer's planned metadata diff (issue #117, comparison
DIDs in the gist linked from the issue) diverges on day one.

**Root cause (confirmed):** `ResolveFromChainAsync` builds metadata with only
`VersionId`/`Deactivated`/`NextVersionId` — `src/NetDid.Method.Ethr/DidEthrMethod.cs:382-389`.
The Core model already has both fields: `DidDocumentMetadata.Updated` (`DateTimeOffset?`)
and `NextUpdate` (`string?`) — `src/NetDid.Core/Model/DidDocumentMetadata.cs:6,11`. No Core
change needed.

**Normative semantics (verified against the did:ethr spec Read section and
`resolver.ts` of the reference implementation):**
- `updated` MUST be the ISO date string of the block time of the latest applied update,
  *without sub-second resolution* (e.g. `2021-03-22T18:14:29Z`). Reference sets it whenever
  `versionId !== 0` (i.e. ≥1 applied event) — on current *and* historical resolution.
- `nextUpdate` MUST be the ISO date string of the block time of the next update; reference
  sets it whenever `nextVersionId` is finite (only possible on `versionId`/`versionTime`
  queries — same condition NetDid already uses for `NextVersionId`).
- Genesis documents (no applied events) omit `versionId`/`updated`/`nextVersionId`/`nextUpdate`.
- `deactivated` merges alongside these fields (a deactivated doc with history has both).

Severity: Medium (interop divergence, no security impact). Matches issue label `enhancement`.

## Out of scope (with justification)

- **Core model/serializer changes** — both fields exist and serialize; the
  `Updated: DateTimeOffset?` vs `NextUpdate: string?` asymmetry is the published Core API;
  changing it is a breaking change unrelated to this issue.
- **Other methods (`did:key`/`did:peer`/`did:webvh`)** — their metadata contracts are
  separate specs; webvh already emits its own version metadata.
- **W3C conformance suite** — `updated`/`nextUpdate` are method-specific resolver outputs,
  not DID-Core document statements.
- **Issue #120 interop fixtures** — the recorded-fixture harness is its own issue (blocked
  on the maintainer's DID list conversation); here I do a best-effort live diff only.
- **RPC error-shape hardening** — untouched; no new response *shapes* are parsed
  (`GetBlockTimestampAsync` already validates and is already used on both historical paths).

## Plan

### 1. Implementation — `src/NetDid.Method.Ethr/DidEthrMethod.cs` (single file)

All inside `ResolveFromChainAsync` plus one small private helper:

1. **Block-timestamp cache**: a local `Dictionary<ulong, ulong>` populated by every
   `GetBlockTimestampAsync` fetch the method already performs:
   - `versionId` path: caches the requested block's timestamp (line ~333).
   - `versionTime` path: caches every walked block's timestamp (line ~349) — so `updated`
     and `nextUpdate` are **free** there (issue's "reuse timestamps already fetched").
   - A tiny `GetBlockTimestampCachedAsync(rpc, cache, block, ct)` helper does
     cache-hit-or-fetch with the mandatory cancellation pattern (token check BEFORE the
     call, `.WaitAsyncObserved(ct)`, token check AFTER the await — PR #122 lessons; the
     final pre-success gate at line ~394 already exists and stays).
2. **Populate `Updated`**: when `lastChangeBlock > 0` (exactly when `VersionId` is set),
   fetch/lookup its timestamp → `Updated = <UTC DateTimeOffset>`. Costs at most one extra
   `eth_getBlockByNumber`: on the default path always one; on the `versionId` path zero when
   the requested block IS the last change, one otherwise; on `versionTime` zero.
3. **Populate `NextUpdate`**: when `nextVersionId` is set, fetch/lookup its timestamp →
   `NextUpdate = "yyyy-MM-dd'T'HH:mm:ss'Z'"` (invariant culture, seconds resolution — the
   reference format). Zero extra fetches on `versionTime`; one on `versionId` queries.
4. **Bounded timestamp conversion helper**: `(long)ts` + `FromUnixTimeSeconds` is used
   today at lines 335/358 — a hostile node returning `> long.MaxValue` wraps negative
   (silent 1969 dates) or throws `ArgumentOutOfRangeException`. Add one helper that rejects
   unix seconds `> 253402300799` (DateTimeOffset max) with `EthereumInteractionException`
   (→ existing `internalError` mapping) and use it at the two existing sites and the new
   ones — same-consumer sibling hardening (lessons: enumerate sibling fields reaching the
   same consumer); genuinely tiny.

The deadline CTS already spans these fetches (they're inside `ResolveFromChainAsync`,
bounded per-await); timestamps are `ulong`s so the retained-byte budget is unaffected.

### 2. Regression tests — fail-first (`tests/NetDid.Method.Ethr.Tests/Issue117MetadataTimestampTests.cs`)

New file following the `Issue116ResolutionErrorMappingTests.cs` convention, NSubstitute
mock RPC per `DidEthrResolveCorrectnessTests` helpers. Matrix:

- Current resolution with history → `Updated` equals last-change block timestamp (exact
  `DateTimeOffset`), `NextUpdate`/`NextVersionId` null.
- Genesis (no events) → `Updated`, `NextUpdate`, `VersionId` all null (criterion 3).
- `versionId` between changes (blocks 10/20) → `Updated` = ts(10), `NextUpdate` = ISO of
  ts(20), exact `...Z` seconds format asserted (criterion 2).
- `versionId` equal to the last change block → `Updated` reuses the already-fetched
  timestamp (assert exactly one `GetBlockTimestampAsync(version, …)` call).
- `versionTime` path → `Updated` + `NextUpdate` populated with **zero additional**
  timestamp calls (NSubstitute `Received` counts — pins the reuse requirement).
- Deactivated DID with history → `Deactivated == true` AND `Updated` set (reference merges).
- Hostile timestamp `> 253402300799` → `internalError`, never a wrapped 1969 date.
- Caller cancellation firing during the NEW terminal `Updated` fetch (completed-task and
  pending-task modes) → propagates as cancellation, no stale success (terminal-path lesson).
- One emulator differential test in `EmulatedEthereumChainTests` (emulator already
  implements `GetBlockTimestampAsync`): update → resolve current + historical, assert
  `updated`/`nextUpdate` match emulator block timestamps.

**Fail-first proof**: run the new tests against stashed pre-fix source, record the red run.

### 3. Best-effort reference diff (criterion 4)

From the scratchpad, attempt a live comparison for 2–3 DIDs from the maintainer's gist
(mainnet/sepolia): run the npm `ethr-did-resolver` via a small node script against a public
RPC, resolve the same DIDs through NetDid (`DefaultEthereumRpcClient`), diff
`versionId`/`updated`/`nextVersionId`/`nextUpdate`. If npm/network/archive access is
unavailable in this environment, record exactly what was compared vs blocked in the plan
review section and the PR (full-list diff then rides on issue #120's fixture harness).

### 4. Verify + adversarial review

- `net-did-verify` skill: 0-warning Release build, full suite per-project counts, W3C
  conformance, samples.
- `adversarial-review` skill on the diff (new RPC-data consumption path in resolution —
  security-relevant), fix CONFIRMED findings, re-verify.

### 5. Docs + CHANGELOG

- `NetDidPRD.md` §8.5 step 5/7: state the metadata contract (`updated` whenever a change
  is applied; `nextUpdate` with `nextVersionId`; format; genesis omission).
- `README.md` historical-resolution snippet (~line 306-321): mention `nextUpdate` next to
  the existing `nextVersionId` example.
- `CHANGELOG.md` `[Unreleased]` → `Added`. **No version bump.**

### 6. PR

Branch `fix/issue-117-metadata-updated-nextupdate` off `main` (created before first edit).
Plan copied to `tasks/todo{timestamp}.md`; commit, push, open PR with `Fixes #117`;
**stop at the open PR** (user reviews/merges). Append `## Review` section with test
counts, fail-first evidence, criteria re-check, and the reference-diff outcome.

## Files touched

| File | Change |
|---|---|
| `src/NetDid.Method.Ethr/DidEthrMethod.cs` | timestamp cache + `Updated`/`NextUpdate` population + bounded conversion helper |
| `tests/NetDid.Method.Ethr.Tests/Issue117MetadataTimestampTests.cs` | new regression matrix |
| `tests/NetDid.Method.Ethr.Tests/EmulatedEthereumChainTests.cs` | one differential metadata test |
| `NetDidPRD.md`, `README.md`, `CHANGELOG.md` | metadata contract docs + changelog |

## Verification

`dotnet build -c Release` (0 warnings) → `dotnet test` all projects with per-project
counts → W3C conformance green → samples run → fail-first red-run evidence recorded →
adversarial review pass → best-effort reference-resolver diff.

## Review

**Files changed** (vs `main`): `src/NetDid.Method.Ethr/DidEthrMethod.cs` (timestamp cache,
`Updated`/`NextUpdate` population, bounded `ToUtcTimestamp` at all four conversion sites,
`FormatIso8601Utc`, deadline-timeout message wording), new
`tests/NetDid.Method.Ethr.Tests/Issue117MetadataTimestampTests.cs` (10 tests), one emulator
differential test in `EmulatedEthereumChainTests.cs`, #109 bounded-await inventory 14→15,
`NetDidPRD.md` §8.5, `README.md`, `CHANGELOG.md` `[Unreleased]` (no version bump).

**Verification (net-did-verify)**: Release build 0 warnings / 0 errors. Tests: Core 377,
WebVh 420, Ethr 428 (was 417 on main; +11 new), W3C conformance 233 (green, report churn
restored), Peer 48, Key 52, DI 18, Ethr integration 11 (+7 skipped, opt-in). All samples
exit 0. `git diff --check` clean.

**Fail-first evidence**: with the fix committed and `DidEthrMethod.cs` reverted to `main`'s
version, the Issue117 suite ran 10 FAILED / 1 passed (the genesis-omission pin, correct
pre-fix by design). Restored, 11/11 green, diff intact.

**Acceptance criteria**:
- [x] Current resolution with history → `updated` = last change block's timestamp (unit +
  emulator differential + live)
- [x] Historical `versionId`/`versionTime` → `nextUpdate` beside `nextVersionId` (unit +
  emulator + live)
- [x] Genesis omits both (unit test; zero timestamp RPC calls asserted)
- [x] Reference-resolver diff: **14/14 DIDs from the maintainer's gist MATCH** live across
  mainnet/sepolia/gno/polygon/aurora (drpc archive endpoints; publicnode gates archive logs —
  relevant to #119). Historical `nextVersionId`/`nextUpdate` consistency verified per DID.

**Adversarial review** (3 independent worktree-isolated agents: cancellation/abandonment,
hostile-node/DoS, reference-conformance): no CONFIRMED exploitable finding; the change also
closes a pre-existing silent timestamp-wrap band (hostile ts near ulong.MaxValue previously
produced pre-1970 instants on historical paths). CONFIRMED-Low cosmetic timeout-message
wording fixed. Conformance Finding 1 (Medium, pre-existing): post-deactivation histories
diverge from the reference's break-at-deactivation replay — deliberate PRD §8.7 stance,
filed as follow-up issue #125; none of the 14 comparison DIDs is affected. Noted (no action,
pre-existing Core shape): `Updated` is `DateTimeOffset` while `NextUpdate` is a
preformatted `Z` string — matches the existing `Created` convention.

**Behavior diff vs main**: metadata gains `updated`/`nextUpdate` per the contract above; the
only other observable changes are internalError (instead of silent 1969 wrap / generic
catch) for out-of-range node timestamps, one extra `eth_getBlockByNumber` on latest-path
resolutions with history (deadline-bounded, fail-closed), and the reworded fixed timeout
message.

**Verdict: clean.**

**PR**: https://github.com/moisesja/net-did/pull/126
