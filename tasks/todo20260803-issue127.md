# Issue #127 — did:webvh: author whole-second versionTimes (Option B) + document created/updated contract (Option A)

## Context

PR #126 preserved did:webvh's fractional `versionTime` in serialized resolution metadata, but DID Core §7.3 **mandates** whole-second `created`/`updated`, so those two fields remain lossy projections of the same fractional instants. For logs whose versions differ only sub-second, serialized `updated` cannot identify the entry it came from (feeding it back as `?versionTime=` mis-selects or returns notFound), and one metadata object renders the same instant two ways.

Issue #127 (milestone 3.1.0) recommends **Option B + A**: make NetDid *author* whole-second `versionTime`s so `created`/`updated`/`versionTime` coincide exactly for NetDid-authored logs, and document that `created`/`updated` are informational, never selectors. Imported fractional logs keep today's read behavior (interop unchanged).

Spec verification (fetched did:webvh v1.0):
- Writer: versionTime "MUST be the time the DID will be retrieved by a witness or resolver, **or before**".
- Resolver: future skew beyond "a small, implementation-defined tolerance… SHOULD use a tolerance of no more than 5 minutes… MUST cause resolution to fail".
- Security Considerations refer to "the **one second** versionTime monotonicity requirement"; all spec examples are whole-second.
- Bonus: `WebVhTimestamp.Format` (`.FFFFFFF`) already emits whole-second values with no fraction/dot, exactly matching `CanonicalUtcDateTimeOffsetJsonConverter` output — no serializer change needed.

Severity: Medium (fidelity/interop defect, not a vulnerability — fractional truth stays authenticated in the log and exposed via `versionTime`/`versionId`).

## Root cause (citations)

- `src/NetDid.Method.WebVh/DidWebVhMethod.cs:148` — Create stamps raw `DateTimeOffset.UtcNow` (fractional).
- `src/NetDid.Method.WebVh/DidWebVhMethod.cs:733-744` — `GetNextVersionTime` advances by `AddTicks(1)` under contention, manufacturing sub-second-distinct versions.
- `src/NetDid.Core/Model/DidDocumentMetadata.cs:8-18` — `Created`/`Updated` whole-second vs `VersionTime` fractional (correct per DID Core; not changing).

## Plan

- [ ] Branch `feat/issue-127-whole-second-versiontime` off `main`; copy plan to `tasks/todo{timestamp}.md`
- [ ] `WebVhTimestamp`: add `internal static DateTimeOffset TruncateToWholeSecond(DateTimeOffset)` (UTC-tick floor)
- [ ] `DidWebVhMethod.CreateCoreAsync` (line 148): `VersionTime = WebVhTimestamp.TruncateToWholeSecond(DateTimeOffset.UtcNow)`
- [ ] `DidWebVhMethod.GetNextVersionTime`: whole-second advance + fail-closed future bound:
  - `nowSecond = TruncateToWholeSecond(UtcNow)`; if `nowSecond > previous` → return `nowSecond`
  - else overflow-guard near `DateTimeOffset.MaxValue` (keep existing `ArgumentException` shape), `next = TruncateToWholeSecond(previous).AddSeconds(1)` — always strictly `> previous`, even for fractional imported previous
  - fail closed: if `next - now > FutureVersionTimeTolerance` (**5 minutes**, matching the spec's resolver SHOULD bound) → `ArgumentException`. Bounds same-second burst skew (issue's caveat "keep k bounded or fail closed") and refuses appending to far-future imported logs
- [ ] Option A docs: XML docs on `DidDocumentMetadata.Created`/`Updated`/`VersionTime` — created/updated are informational (DID Core §7.3 whole-second mandate); `versionTime`/`versionId` are the only version selectors; never feed `updated` back as a `?versionTime=` query
- [ ] Regression tests `Issue127_*` (region in `TimestampSecurityTests.cs`), fail-first proven pre-fix:
  - Create emits whole-second `VersionTime`; serialized `created` == serialized `versionTime` string
  - Burst of rapid updates in one second → strictly increasing whole-second versionTimes; chain validates; log round-trips
  - **Headline**: after burst, serialized metadata `updated` string reused as `?versionTime=` selector re-selects the same `VersionId`
  - Fractional *imported* log (hand-built via the `CreateAuthenticatedChainAsync`/`SignEntryAsync` pattern from `LogChainValidatorTimestampTests.cs:198-255`): still resolves/selects at full precision; serialized `created`/`updated` pinned as documented whole-second lossy, `versionTime` fractional
  - Future tolerance: previous beyond `now + 5min` → Update/Deactivate fail closed with `ArgumentException`
- [ ] Rework 2 existing pins (property preserved, proxy updated):
  - `Issue117_ResolvedVersionTime_ReSelectsSameFractionalWebVhVersion` (`TimestampSecurityTests.cs:280`) asserts Create emits *fractional* — rebuild its fractional log via the authenticated-chain helper instead of Create output; the round-trip property stays pinned
  - `Update_FutureDatedCurrentLog_StillEmitsStrictlyIncreasingVersionTime` (`LogChainValidatorTimestampTests.cs:180`) uses +5min previous — move inside tolerance (+4min); the beyond-tolerance case becomes the new negative test
- [ ] Verify via `net-did-verify` (full suite — a Core-adjacent timestamp change must run every method's tests per lessons)
- [ ] `adversarial-review` on the diff
- [ ] Docs: `NetDidPRD.md` §7 timestamp section (~line 1128) — authoring is whole-second, reading preserves fractional; `README.md` webvh notes; `CHANGELOG.md` `[Unreleased]` → Changed. No `NetDidVersion` bump
- [ ] File spin-off issue: resolver does not enforce the spec's future-skew MUST ("entries that exceed this tolerance MUST cause resolution to fail") — read-side gap, out of scope here (precedent: #125)
- [ ] Commit, push, open PR with `Fixes #127`; stop at open PR; append `## Review` to plan file

## Out of scope (with justification)

- **Read-side future-tolerance enforcement** — distinct conformance gap in resolution; behavior change for fetched logs; tracked via spin-off issue.
- **Core converters / `DidDocumentMetadata` serialization policy** — DID Core mandates the whole-second form; PR #126 settled the split policy; unchanged.
- **did:ethr** — never sets fractional instants (block timestamps are whole-second by consensus); untouched; full-suite run is the cross-method guard.
- **Fractional read path (`WebVhTimestamp.TryParse`, `FindTargetIndex`, chain validation)** — interop with other implementations' fractional logs must keep working; deliberately unchanged.

## Files touched

| File | Change |
|---|---|
| `src/NetDid.Method.WebVh/WebVhTimestamp.cs` | add `TruncateToWholeSecond` |
| `src/NetDid.Method.WebVh/DidWebVhMethod.cs` | whole-second Create stamp; rewritten `GetNextVersionTime` with 5-min fail-closed bound |
| `src/NetDid.Core/Model/DidDocumentMetadata.cs` | XML docs only (Option A contract) |
| `tests/NetDid.Method.WebVh.Tests/TimestampSecurityTests.cs` | `Issue127_*` region; rework Issue117 fractional pin |
| `tests/NetDid.Method.WebVh.Tests/LogChainValidatorTimestampTests.cs` | future-dated pin moved inside tolerance + new negative |
| `NetDidPRD.md`, `README.md`, `CHANGELOG.md` | policy docs + changelog |

## Verification

1. Fail-first: stash fix, run new `Issue127_*` tests, confirm assertion failures (fractional authoring / mis-selection), restore.
2. `net-did-verify`: 0-warning Release build, full per-project test counts, W3C conformance green, samples end-to-end, `git diff --check` clean.
3. `adversarial-review` on the completed diff; fix CONFIRMED findings, re-verify.
